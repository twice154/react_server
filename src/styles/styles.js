import styled from 'styled-components'

export const Input = styled.input`
font-family: Cabin;
		font-size: 16px;
		font-weight: 500;
		font-style: normal;
		font-stretch: normal;
		line-height: normal;
		letter-spacing: 0.3px;
        text-align: left;
        padding-left: 10px
`